<UserControl x:Class="FindMePrism.Views.ViewLosts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FindMePrism.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="520" d:DesignWidth="700">

    <Border Background="#283965" Opacity=".72">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

                <ListBox HorizontalContentAlignment="Stretch" Grid.Row="0"  Background="#283965"
            ItemsSource="{Binding Losts}"           
            ItemTemplate="{StaticResource LostItemTemplate}"/>
                <Button Grid.Row="1" Style="{StaticResource AddButtonStyle}" Command="{Binding AddLostCommand}" Content="ADD"/>
        </Grid>
    </Border>
</UserControl>
